# MMNTUM Homebrew Tap

```bash
brew tap mmntum-llc/tap
brew install mmntum
```

Casks in `Casks/` are published automatically by
[mmntum-cli](https://github.com/mmntum-llc/mmntum-cli)'s release workflow
(GoReleaser) on every `v*` tag — don't edit them by hand.
